{
  "name": "inzo-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Agent-to-agent coordination, with humans in the loop.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w inzo-holder && npm run build -w inzo-sandbox && npm run build -w inzo-relay && npm run build -w inzo-mcp && npm run build -w inzo-cli",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run build && npm run test --workspaces --if-present",
    "test:only": "npm run test --workspaces --if-present",
    "dev:relay": "npm run dev -w inzo-relay"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}